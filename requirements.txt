# CUDA-enabled PyTorch + project dependencies (default for GPU training).
# cu128 supports recent NVIDIA GPUs (e.g. RTX 40xx / 50xx).
# Install: pip install -r requirements.txt
#
# Use Python 3.11 or 3.12 for best GPU wheel compatibility.
# Use PyTorch index as primary so pip does not pick the CPU wheel from PyPI.

--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple

torch>=2.4.0
transformers>=4.47.0
peft>=0.13.0
trl>=0.9.0
accelerate>=1.0.0
bitsandbytes>=0.43.0
datasets>=3.0.0
